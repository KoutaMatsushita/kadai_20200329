micronaut:
  application:
    name: kadai20200329
datasources:
  default:
    url: ${DATABASE_URL}
    driverClassName: org.postgresql.Driver
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
flyway:
  datasources:
    default:
      enabled: true
